<?php include 'header.phtml'; ?>
            <li class="breadcrumb-item active" aria-current="page">Users listing</li>
        </ol>
    </nav>

    <div class="container">
        <!--Page title block-->
        <div class="text-center">
            <h1>Users listing</h1>
        </div>

        <!--Table actions block-->
        <div class="row">
            <div class="col-12 col-md-6">
                <form action="">
                    <div class="form-group">
                        <label for="roleSearch">User role select</label>
                        <select class="form-control" id="roleSearch">
                            <option>admin</option>
                            <option>user</option>
                        </select>
                    </div>
                </form>
            </div>
            <div class="col-12 col-md-6 d-flex align-items-end justify-content-end">
                <a class="btn btn-primary py-1 mb-3" href="/user/add">Add new user</a>
            </div>
        </div>

        <!--Quiz listing table-->
        <div class="table-responsive my-4">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <!--Sorting arrow are displayed using the 'asc','dsc' classes-->
                    <th scope="col" class="sorting-header asc dsc">ID</th>
                    <th scope="col">Email</th>
                    <th scope="col">Role</th>
                    <th scope="col">Actions</th>
                </tr>
                </thead>
                <tbody>
                <?php
                if (isset($entities)) {
                    foreach ($entities as $key => $values) {
                        ;
                        echo '<tr>
                    <th scope="row">' . $values->getId() . '</th>
                    <td>' . $values->getName() . '</td>
                    <td>' . $values->getRole() . '</td>
                   
                    <td class="text-center">
                        <a class="btn btn-primary py-1" href="/user/add/' . $values->getId() . '">Edit</a>
                        <a class="btn btn-danger py-1" href="delete/' . $values->getId() . '">Delete</a>
                    </td>
                </tr>';
                    }
                }
                ?>
                </tbody>
            </table>
        </div>

        <!--Pagination block-->
        <nav aria-label="Page navigation example">
            <ul class="pagination justify-content-center">
                <li class="page-item disabled">
                    <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
                </li>
                <li class="page-item"><a class="page-link" href="#">1</a></li>
                <li class="page-item active"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item">
                    <a class="page-link" href="#">Next</a>
                </li>
            </ul>
        </nav>
    </div>
</body>
</html>