<?php use QuizApp\DTOs\QuestionDTO;

include "header.phtml"; ?>
            <li class="breadcrumb-item active" aria-current="page">Results</li>
        </ol>
    </nav>

    <div class="container">
        <!--Page title block-->
        <div class="text-center">
            <h1>Quiz ...</h1>
        </div>

        <!--UserController details block-->
        <div class="user-block mb-3">
            <span>User: </span>
            <span class="h5"><?php echo $session->get('name'); ?></span>
        </div>
        <!--Quiz block-->
        <?php
        /** @var QuestionDTO $question */
        foreach ($questions as $question): ?>
            <div class="card border-secondary mb-3">
                <div class="card-header">
                    <p class="mb-1">Question: </p>
                    <span><?php echo $question->getQuestionText(); ?></span>
                </div>
                <div class="card-body">
                    <p class="mb-1">Answer: </p>
                    <span><?php echo $question->getAnswerText(); ?></span>
                </div>
            </div>
        <?php endforeach; ?>
                <a class="btn btn-primary" href="/congrats">Save</a>
    </div>
</body>
</html>